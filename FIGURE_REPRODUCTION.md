# Figure Reproduction Guide
Processed datasets in `processed_data/` can be used to regenerate Figure-like outputs using the scripts in `scripts/`.

Example:
- Figure RDF -> `rdf_analysis.py`
- Figure MSD -> `msd_analysis.py`
- Arrhenius plot -> `arrhenius_fit.py`
